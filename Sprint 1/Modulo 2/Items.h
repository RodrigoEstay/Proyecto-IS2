#ifndef ITEMS_H
#define ITEMS_H
#include "ResAp.h"
#include <vector>
	/*
		Representa Items.h para que el codigo compile. Despues reemplazar con los archivos correspondientes
	*/
class Items{
	public:
		Items(int num_evaluacion, int num_item, int puntos){
			
		}
		vector<ResAp*> getResultadosAsignados(){
			return vector<ResAp*>();
		}

};

#endif